#include <cs50.h>
#include <stdio.h>

int main(void)
{
  int calls = 4;
  printf("calls is %i\n", calls);
  // %i is called format code
  // calls is the value that %i is going to calls
  // there are many difference types of format codes
}

